<!--<div class="container mt-5">
  <ul class="list-group">
    {{#each users as |user|}}
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <span>{{ user.email }} <span class="badge {{#hasRole user 'admin'}} badge-danger {{else}} badge-secondary {{/hasRole}}">{{ user.role }}</span></span>
        {{ user.name }}
        {{#hasRole ../session 'admin'}}
          <form action="/users/{{ user.id }}/delete" method="POST">
            <button class="btn btn-link"><i class="fas fa-times text-danger"></i></button>
          </form>
        {{/hasRole}}
      </li>
    {{/each}}
  </ul>
</div>-->
<div class="UserList">
  {{#each users as |user|}}
  <div class="UserList__item">
    <div class="UserList__item-avatar">
      <img src="{{ user.avatarURL }}" class="Profile__avatar" alt="{{ user.name }}">
    </div>
    <div class="UserList__item-name">{{ user.name }}</div>
  </div>
  {{/each}}
</div>
