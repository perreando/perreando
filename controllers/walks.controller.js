const Walk = require('../models/walks.model');
const createError = require('http-errors');
const mongoose = require('mongoose');

// module.exports.list = (req, res, next) => {
//   res.render()
// }

module.exports.create = (req, res, next) => {

}